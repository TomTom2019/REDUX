import { createSlice } from "@reduxjs/toolkit";

// CRETED REDUCER  keyword is movies
export const moviesSlice = createSlice({
	name:'movies',
	initialState:{},
     reducers:{

     }

})


export default moviesSlice.reducer